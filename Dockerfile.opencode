FROM oven/bun:latest

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

RUN bun install -g opencode-ai@1.2.6

EXPOSE 54321

CMD ["opencode", "serve", "--port", "54321", "--hostname", "0.0.0.0"]
